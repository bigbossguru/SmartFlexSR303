STARTUP=#!/bin/sh
STARTUP=#
STARTUP=# This script will be executed *after* all the other init scripts.
STARTUP=# You can put your own initialization stuff in here.
STARTUP=
STARTUP=iptables -t nat -A POSTROUTING -o eth2 -j post